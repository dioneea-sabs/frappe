# Shared MariaDB for ALL Frappe benches on this server.
# This same password is the MariaDB root password and must match DB_PASSWORD
# in every bench's env file (helpdesk.env, etc).
DB_PASSWORD=CHANGE_ME_strong_db_root_password
